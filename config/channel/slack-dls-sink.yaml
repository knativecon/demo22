apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: slack-dls
spec:
  template:
    spec:
      containers:
        - image: ko://knativecon/demo22/cmd/slack-sink
          env:
            - name: SLACK_CHANNEL
              value: kn-dls
            - name: SLACK_TOKEN
              valueFrom:
                secretKeyRef:
                  name: slack-secret
                  key: token
